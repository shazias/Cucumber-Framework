# wait values

page.load.timeout = 60
implicit.wait.timeout = 30


# browser to use when running locally
browser.name = chrome

os.name = linux

# Execute on grid
execute.remotely = true

# Environment to execute on
environment.name = uat


ui = true
